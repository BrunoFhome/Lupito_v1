spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.username=postgres
spring.datasource.password=0cd76381
spring.datasource.url=jdbc:postgresql://localhost:5432/lupitov1

spring.jpa.hibernate.ddl-auto=none
spring.jpa.generate-ddl=false
spring.jpa.show-sql=true

spring.flyway.enabled=true

server.port=8081

jwt.secret=${JWT_SECRET:lupito-dev-secret-change-in-production}

# URL do frontend (para o link de reset no email)
app.frontend.url=${FRONTEND_URL:http://localhost:4200}

# Configurações de email — substitua os valores em application-local.properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Importa arquivo local de credenciais (não vai para o Git)
spring.config.import=optional:classpath:application-local.properties